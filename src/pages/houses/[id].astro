---
import DefaultLayout from '../../layouts/DefaultLayout.astro'
import HouseLoader from '../../components/houses/HouseLoader.vue'

const { id } = Astro.params
---

<DefaultLayout title="Houses of Ice and Fire">
  <div class="container mx-auto px-4 flex-auto flex">
    <HouseLoader client:only id={id} />
  </div>
</DefaultLayout>
